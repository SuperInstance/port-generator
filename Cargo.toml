[package]
name = "port-generator"
version = "0.1.0"
edition = "2021"
description = "Cross-language port generator from Rust API definitions for the Grand Pattern ecosystem"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
